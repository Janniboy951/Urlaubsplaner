import { StyleSheet } from "react-native";

export default StyleSheet.create({
  centeredDialog: {
    padding: 0,
    margin: 0,
    justifyContent: "center",
    alignContent: "center",
    alignItems: "center",
    textAlign: "center",
  },
});
